import { typographyStyles as typography } from './typography';
import { layoutStyles as layout } from './layout';
import { componentStyles as components } from './components';

export const globalStyles = {
  typography,
  layout,
  components,
};

export default globalStyles;
